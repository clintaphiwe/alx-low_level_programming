#include "main.h"

/**
 * main - Entry Point
 * positive_or_negative() - Function that tests the correct output given.
 * Return: 0 (Success)
 */

int main(void)
{
int a;

a = 0;
positive_or_negative(a);

return (0);
}
