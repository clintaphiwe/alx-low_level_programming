#include <stdio.h>
#include "main.h"

/**
 * _abs - Function that returns the absolute value of a number
 * @n: Input
 * Return: -n (success)
*/

int _abs(int n)
{
if (n >= 0)
return(n);
return (-n);
}
