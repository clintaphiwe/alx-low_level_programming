#include <stdio.h>

/**
 * main - The entry point.
 * Return: Will print the output using the 'printf' function.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
